<!DOCTYPE html>
<html>
  <head>
    <title>TestSuite<% if @nav %> - <%= @nav.capitalize %><% end %></title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="/css/testsuite.css" />
    <%# stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application' %>
  </head>
  <body<% if @nav == "form" %> style="width: 1550px"<% end %>>
    <section id="head"<% if @nav == "form" %> style="width: 1550px"<% end %>>
    <h1>TestSuite</h1>
    <%= render 'layouts/navigation' %>
    <br style="clear: both;" />
    <%= yield %>
  </section>
    <br style="clear: both;" />
  <footer>
    <span id="cpr">&copy; tz <span id="cpr_year"><%= Date.current.year %></span></span>
    <% if current_user %>
    Logged in as <%= link_to current_user.name, current_user %>. | <%= link_to 'Log out', logout_path %>
    <% end %>
  </footer>
  </body>
</html>